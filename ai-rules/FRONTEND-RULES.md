# Frontend Rules

- Use React Query
- Use TypeScript strict mode
- Feature-based structure
- Avoid business logic in components
- Handle loading/error states
- Use reusable validation
- Protect routes by RBAC